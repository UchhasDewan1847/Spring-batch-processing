spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/file_upload
    username: udewan
    password: root
  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: false
server:
  port: 9090